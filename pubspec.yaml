name: demo
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: ^3.9.2


dependencies:
  flutter:
    sdk: flutter
  #状态管理
  get: ^4.6.6
  #网络请求
  dio: ^5.4.0
  # 腾讯云 云开发
  cloudbase_flutter: ^1.0.3
  # 地图（基于 OpenStreetMap，无需 API Key）
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  # 定位
  geolocator: ^13.0.2
  # 点击 Marker 打开外部地图软件导航
  url_launcher: ^6.3.1
  #权限
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:


  uses-material-design: true


